<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:background="?attr/selectableItemBackground"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:id="@+id/root_layout"
    android:padding="8dp">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/repo_title_text_view"
        android:textColor="@color/primary_text"
        android:maxLength="30"
        android:scrollHorizontally="true"
        android:ellipsize="end"
        android:textStyle="bold"
        android:textSize="14sp" />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/programming_language_text_view"
        android:textColor="@color/secondary_text"
        android:textSize="12sp"
        android:maxLines="1"
        android:lines="1"
        android:maxLength="30"
        android:ellipsize="end"
        android:layout_marginTop="4dp" />
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:id="@+id/metrics_panel_layout"
        android:layout_marginTop="4dp">
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:id="@+id/stargazers_count_text_view"
            android:textColor="@color/secondary_text"
            android:textSize="12sp"
            android:drawableStart="@drawable/ic_metric_star"
            android:drawablePadding="4dp" />
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:id="@+id/forks_count_text_view"
            android:textColor="@color/secondary_text"
            android:textSize="12sp"
            android:drawableStart="@drawable/ic_metric_fork"
            android:drawablePadding="4dp" />
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:id="@+id/watchers_count_text_view"
            android:textColor="@color/secondary_text"
            android:textSize="12sp"
            android:drawableStart="@drawable/ic_metric_watch"
            android:drawablePadding="4dp" />
    </LinearLayout>
</LinearLayout>